{
  "name": "kestrel",
  "version": "0.1.0",
  "private": true,
  "description": "WebXR viewer for spatial scientific data, and the wind-modelling API that feeds it",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "build": "npm run build --workspace server",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspace server"
  }
}
